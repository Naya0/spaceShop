export const sumBy = (arr: number[]) => {
  return arr.reduce((prev, cur) => prev + cur, 0);
};

export const totalQuantity = (arr: number[]) => {
  return arr.reduce((prev, cur) => prev + cur, 0);
};
